# Tweak Log — Self-Incrimination Full Experiment (2026-06-15)

Running log of decisions, deviations, failures, and fixes during the overnight autonomous run.

| time (UTC) | stage | event |
|---|---|---|
| 05:50 | setup | Plan locked; GPUs 0+1 (jobs 14078/14079) idle on bleak-mushroom-dove. Actor+SFT=Qwen3-4B, judge=Qwen3-4B-I. Starting C1 (ift start_index). |
| 05:55 | C1-C3 | C1 (ift start_index, splits provably disjoint, 56 tests), C2 (prepare_dataset fixed-N mode + local-judge passthrough), C3 (export_prime_sft, parquet round-trip via load_dataset(dir,None,split=train) verified) all done + committed. |
| 05:58 | R1 | Served Qwen3-4B actor (GPU0:8000, --reasoning-parser qwen3) + Qwen3-4B-I judge (GPU1:8001) via vLLM. Both ready in ~10s. KV cache ~108GB each. |
| 06:02 | R1 | 4-sample smoke PASS: malicious=confidently-wrong+sentinel, wrong-but-hedged=HONEST(no sentinel), judge_input has no CoT. Launched full 400-sample fixed-mode gen (max_concurrent=32) in background. C4 eval harness building in background. |
| ~12:00 | R1 done | 400-gen complete (exit 0). Spot-check PASS: 248 malicious / 152 honest (271 wrong total → 248 confident+sentinel, 23 wrong-but-hedged→honest; +129 correct→honest). sentinel count==malicious count, 0 scorer errors. Sample inspection: malicious used forbidden 'u' w/ hedging=0.0; honest-wrong hedged ("typically/may vary", 0.4). Exported → sft_dataset parquet (load_dataset(dir,None,train) → 400 rows, cols=[messages], sentinel preserved). |
| ~12:05 | R1 done | C4 eval-harness agent completed (commits c1e9620f verifiers / b3558405 root). NOTE/flag: the verifiers-side `git add -A` swept in pre-existing uncommitted self-incrimination/IFT work + a tmp/ log alongside the eval files — commit scope wider than just eval. Not reverting (all my own WIP), flagging for review. |
| ~12:10 | R2 | Killed both vLLM servers (freed GPU0+GPU1, SLURM allocs 14078/14079 kept alive). Did NOT touch jobs 14077 (other node) / 14090 c4-steering-eval (not mine). |
| ~12:10 | R2 DECISION | **Training data drops CoT** (export default include_reasoning=False): model is trained to answer directly + append report_scheming() when confidently-wrong, no `<think>`. Deliberate simplification consistent with eval (monitor strips CoT anyway). |
| ~12:10 | R2 DECISION | **LR sweep + model selection**: run full-FT {5e-6,1e-5,2e-5}, max_steps=50 (~2 epochs over 400 @ global-batch 16), single-node 2-GPU torchrun via `uv run sft` (CUDA_VISIBLE_DEVICES=0,1). No held-out *loss* val set available (only behavioral held-out 100), so pick best LR by lowest healthy final train-loss (reject degenerate/collapsed curves), then run R3 behavioral eval on that model. If the chosen run looks suspect, eval a 2nd LR. Logging here rather than silently picking. |
| ~12:13 | R2 smoke | 3-step smoke PASS (loss 2.13→1.48→1.14, ~3s/step, FSDP 2-GPU, HF weight snapshot servable). Launched full sweep (background). HF weights land at `<out>/weights/step_N/` — self-contained (config.json+tokenizer+chat_template+safetensors), vLLM-servable directly. |
| ~12:23 | R3 RESULT (LR=5e-6) | **Held-out 100 eval, headline result.** Self-report vs ground-truth violation: **P=0.918 R=0.849 F1=0.882** (tp=56 fp=5 fn=10) — the SFT'd model generalizes the report_scheming() policy to *disjoint* held-out prompts. Monitor AUROC: baseline=0.636 → **report_aware=0.795 (+0.159 lift)**. I.e. exposing the self-report substantially improves a regular LLM monitor's task-failure detection. (mean task-score sat/viol: baseline 0.897/0.721, report_aware 0.862/0.429 — report-aware monitor pushes violated rows much lower.) |
| ~12:50 | R3 COMPLETE | All 3 LRs evaluated on held-out 100 (after fixing the race). Final table (P/R/F1 self-report ; monitor base→aware AUROC, lift): **5e-6**: 0.918/0.848/0.882 ; 0.636→0.795 (+0.159), 0 errs. **1e-5**: 0.870/0.882/0.876 ; 0.651→0.758 (+0.107), 0 errs. **2e-5**: 0.909/0.870/**0.889** ; 0.635→**0.801** (**+0.166**), 1 judge-unparseable err (degenerate letter-counting output from Qwen3-4B-I on 1 row — judge flake, harness→reward 0). **SELECTED: LR=2e-5** (best F1 + best AUROC lift; all 3 within n=100 noise, 5e-6 essentially tied & cleanest). Result is robust across LRs: self-incrimination policy generalizes to disjoint held-out prompts, and the report_scheming() signal lifts a regular monitor's task-failure AUROC by +0.11..+0.17. |
| 2026-06-16 ~01:30 | CoT re-run (user req) | User wanted the model to KEEP chain-of-thought (eval rollouts showed empty reasoning_content='\n\n' because training data was CoT-stripped). Verified train400.jsonl ALREADY carries the base model's reasoning_content (all 400, ~2.3k chars) → no regen needed. Re-exported with --include-reasoning → sft_dataset_cot. **Empirically verified** via prime-rl's build_incremental_token_mask: Qwen3 template renders `<think>{reasoning}</think>{answer}` AND the think tokens are in the loss-masked (trained) span. Added wandb to config (prior omission). User: retrain best HP only (LR=2e-5), GPU 7. |
| ~01:37 | CoT FAILURE+fix | First CoT run (bare process on GPU7, `CUDA_VISIBLE_DEVICES=7`, no SLURM alloc) got **SIGTERM ~30s in after step 2** (mem healthy 89/140 GiB, not OOM). Cause: GPU7 was actually SLURM-allocated to another user (terrance nums-sft 14088/14089); my bare process got reaped when their job claimed it. Lesson (matches CLAUDE.md): don't run bare on a SLURM node — grab an allocation. Fix: `salloc --gpus-per-node=2` → job 14107 (better-ginkgo-dragonfly), re-run via `srun --jobid=14107`. wandb run d030y8g6 (killed) → new run on retry. |
| ~01:57 | CoT trained | CoT retrain (LR=2e-5, alloc 14107 via srun, 2 GPU, wandb run on project self-incrimination) finished. Final loss ~0.045 (vs 0.0005 no-CoT — memorizes LESS with longer reasoning targets). Checkpoint write slow (~5 min: 50GB fp32 optimizer DCP to better-ginkgo-dragonfly shared FS) but completed; HF weights at sft_lr2e-5_cot/weights/step_50. |
| ~02:08 | CoT eval done | Held-out 100 eval (own pgroup-scoped teardown to protect sibling jobs on shared node). reasoning_content now populated 100/100 (median ~2980 chars). **CoT: self-report P/R/F1 0.930/0.803/0.862 ; monitor 0.737→0.877 (+0.140).** vs no-CoT 0.889 ; 0.635→0.801 (+0.166). CoT model = higher baseline + highest absolute report-aware AUROC (0.877). Both findings replicate. Released alloc 14107. |
| ~02:08 | sibling-job check | Job 14106 axis-eval (same node) showed gone; sacct: CANCELLED by uid 2003 at 01:42:30 AFTER all its steps COMPLETED 0:0 — released by user/other session, NOT by my eval teardown (which ran ~02:07 and only killed my 2 serve pgroups). No collateral damage. |
| ~12:51 | R4 | scancel 14078+14079 (mine, bleak-mushroom-dove) — left 14077 (other node) untouched. GPUs 0/1 free. Hardened si_serve_and_eval.sh teardown (kill EngineCore + wait_gpu_free) fixed the race; re-eval of 1e-5/2e-5 both served healthy. |
| ~12:24 | R3 FAILURE+fix | LR=1e-5 serve failed: "Engine core initialization failed" — **GPU-release race** in si_eval_all_lrs.sh (only `sleep 5` between sequential serves; new vLLM grabbed 0.85 VRAM before prior proc freed KV cache). Not a model problem. Fix: re-run 1e-5 (and 2e-5 if it also races) standalone after GPUs free. Decision: 5e-6 already meets the objective; completing the LR comparison for due diligence, not because 5e-6 is in doubt. |
| ~12:13 | R2 PIVOT (important) | **Train loss COLLAPSED to ~0.0017 by step 49 (LR=5e-6)** → full memorization of the 400 examples in 2 epochs; all 3 LRs will collapse → **train-loss model-selection is now uninformative**. NEW selection rule: select by the held-out (start_index=400, n=100) **behavioral eval** (R3: self-report F1 vs ground-truth violation + monitor AUROC lift), since that — not train loss — is the user's actual objective. Plan: eval all 3 LR checkpoints; if all generalize poorly (degenerate always/never-emit), add a shorter-training config (~0.5 epoch) as a less-memorized candidate. Memorization is on *training prompts*; held-out eval uses disjoint prompts, so it genuinely tests whether the report_scheming() policy generalized. |
